name: Release action
# Call the reusable workflow release-solution-with-inputs.yml
# Release your solution to prod when you create a new release.

on:
  release:
      types: [created]        
permissions:
  contents: write
jobs:
  Release-solution-ALMLab:
    uses: ./.github/workflows/release-solution-to-prod-with-inputs.yml
    with:
      #You can specify the solution name here
      solution_name: ALMLab
      #Update your values here
      BUILD_ENVIRONMENT_URL: 'https://org6cf0e4e8.crm17.dynamics.com'
      PRODUCTION_ENVIRONMENT_URL: 'https://org9ac4eaea.crm17.dynamics.com'
      CLIENT_ID: '794d3ab2-6afd-476b-a684-6e1951c0dec8'
      TENANT_ID: '25467c26-77bd-45e1-9df5-1fe15127245f'      
    secrets:
      envSecret: ${{ secrets.PowerPlatformSPN }}
